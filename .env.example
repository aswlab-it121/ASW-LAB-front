# Relative path uses Vite dev proxy (see vite.config.ts; default target is http://127.0.0.1:8000).
# Or point directly at a running API, e.g. VITE_API_BASE_URL=http://127.0.0.1:8000/api
VITE_API_BASE_URL=/api
